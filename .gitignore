# --- Python / RAPIDS ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# --- Data & models (large; keep out of git) ---
# Top-level data/ dumps only — NOT the waspada/data/ source package.
/data/
*.csv
*.parquet
*.feather
*.zip
models/
*.pkl
*.joblib

# --- Secrets / GCP credentials (NEVER commit) ---
.env
.env.*
!.env.example
service-account*.json
gcp-*.json
credentials*.json
*-key.json

# --- Node / frontend (Kirana's dashboard) ---
node_modules/
dist/
.vite/

# --- OS / editor ---
.DS_Store
Thumbs.db

*.pptx