# WASPADA — Python dependencies
# GPU deps (RAPIDS) need the NVIDIA package index. Install everything with:
#   pip install --extra-index-url=https://pypi.nvidia.com -r requirements.txt
# (RAPIDS runs on Linux/WSL — see gpu/run_features.py + waspada/wsl.py.)

# --- NVIDIA acceleration layer (the GPU feature-engineering path) ---
cudf-cu12>=26.6        # GPU dataframes (cudf.pandas)
cuml-cu12>=26.6        # GPU ML (risk models)

# --- Alibaba Cloud data layer ---
oss2>=2.18              # OSS bucket client (the loan-portfolio Parquet object)
aliyun-log-python-sdk>=0.9  # Simple Log Service audit stream (WA-023); optional — fail-safe local fallback if absent

# --- Lakehouse access layer (WA-029): dlt load + DuckDB for the Data Engineer agent ---
dlt[duckdb]>=1.4        # dlt load (filesystem source over OSS/S3 endpoint) + DuckDB destination
duckdb>=1.0             # in-process SQL engine the Data Engineer quality tools query

# --- Core pipeline (features + risk model) ---
pandas>=2.2
scikit-learn>=1.5
pyarrow>=17

# --- Agent Society (Qwen Cloud negotiation brain + MCP tool serving) ---
openai>=1.0             # points at Alibaba Cloud DashScope's compatible-mode endpoint
mcp>=1.0                # local MCP server/client for the portfolio_stats tool

# --- Utilities ---
python-dotenv>=1.0

# --- Auth (WA-028): JWT sessions + bcrypt password hashing ---
PyJWT>=2.8
passlib[bcrypt]>=1.7
# passlib 1.7.4 predates bcrypt's 4.1 API change and breaks on bcrypt>=4.1
# ("module 'bcrypt' has no attribute '__about__'") — pin to the last
# compatible line (mirrors api/requirements.txt; fatal in prod, not just CI).
bcrypt>=4.0,<4.1
email-validator>=2.0
# ApsaraDB RDS MySQL driver. Only needed when DATABASE_URL points at a
# mysql:// / mysql+pymysql:// URL; the local-dev / test path uses stdlib sqlite3.
pymysql>=1.1
