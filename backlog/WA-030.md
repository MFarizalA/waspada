---
id: WA-030
state: todo
priority: P1
owner: backend
branch: wa-030-data-analyst-agent
contract_frozen: false
---

# WA-030 · Data Analyst agent (qwen3.7-plus function-calling + DuckDB SQL)

## Goal

Promote `analytics` into a Tier-2 Data Analyst agent: a `qwen3.7-plus`
function-calling loop over DuckDB SQL that *explores* the book for the aggregates
the debate later cites, while still emitting the frozen `FeatureFrame`. The fixed
feature recipe (`build_features`) remains the deterministic core; the aggregates
the agent computes back the MCP evidence base (§ Analytics), so what an agent
cites downstream is what this step computed.

## THE LOAD-BEARING DECISION (do not get this wrong)

The LLM chooses **which explorations to run**. The LLM does **not** compute the
`FeatureFrame` numbers. The `FeatureFrame` is still produced by `build_features()`
so the output is byte-for-byte reproducible — that is the "regression guard"
acceptance below. The agent therefore has **two** outputs:

1. `feature_frame` handle — identical to what `AnalyticsAgent` produces today
   (via `build_features`). Non-negotiable; a regression test pins it. **Handle
   name stays `feature_frame`** — `risk_model` and the whole chain depend on it.
2. `analyst_aggregates` handle (new) — the correlations/distributions/group-bys
   the agent chose to run, stashed on the context to back the MCP evidence base.
   This is the new value the LLM adds. It must never feed back into the feature
   numbers.

Loop failure / unparsable brain / brain unavailable → fall back to plain
`build_features` + empty aggregates. Pipeline never crashes; contract always
satisfied.

## Files / interfaces

- **New:** `waspada/agents/data_analyst.py` — `DataAnalystAgent`
  (`name = "data_analyst"`, brain defaults to `MockLLM()`, tier `qwen3.7-plus`).
  **Mirror `waspada/agents/data_engineer.py`** — it is the working reference for
  the Qwen function-calling loop (`_reasoning_loop`, `_parse_tool_call`,
  `register_tool`, hop budget, per-hop `self.step(...)` logging,
  unparsable-degrades-to-defaults). Copy the `_arrow_lakehouse` helper (duckdb
  `:memory:` + `con.register`); do not import a private symbol.
- **Edit:** `waspada/agents/orchestrator.py` — swap `"analytics"` →
  `"data_analyst"` in `COLLECTIONS_STEP_ORDER`; in `_build_agents()` construct
  `DataAnalystAgent(self.llm.with_model("qwen3.7-plus"), as_of=self.as_of)` in
  place of `AnalyticsAgent(...)`. Keep `analytics.py` — it stays as the
  deterministic core the new agent calls; do not delete it.
- **Edit:** `waspada/agents/__init__.py` — export `DataAnalystAgent`.

## Tools to register (read-only DuckDB SQL; `register_tool` pattern)

Each takes the `Lakehouse` (built over RawLoans+FeatureFrame in `run()`) and
returns a JSON-serialisable dict. **SELECT-only** — reject any non-SELECT SQL
(`.strip().lower().startswith("select")` + reject `;`-chained statements; brain
output is untrusted text).

- `query`         — a bounded `SELECT` the LLM composes (cap rows, e.g. `LIMIT 200`).
- `correlation`   — Pearson corr between two numeric columns (`corr(a,b)`).
- `distribution`  — quantiles/min/max/mean/histogram buckets for one column.
- `build_feature` — return an aggregate the debate cites (e.g. segment medians),
  **NOT** a mutation of the FeatureFrame. Read-only aggregate computation.

## Order of operations in `run()`

1. Consume the predecessor `raw_loans` handle (same predecessor-lookup pattern
   `AnalyticsAgent.run` uses).
2. Build `frame = build_features(raw, as_of)` + `assert_no_nulls`
   **unconditionally, first** — guarantees the contract regardless of the loop.
3. Build a `Lakehouse` over `raw` (and optionally the frame) for the tools.
4. Run the exploration loop → collect `aggregates`.
5. Publish `context.data_handles["feature_frame"] = frame` and
   `context.data_handles["analyst_aggregates"] = aggregates`.
6. Return `AgentResult(status=OK, artifact_ref="feature_frame", ...)`.

## Acceptance

- Real multi-hop tool loop (Qwen decides the next query from prior results),
  logged step-by-step in `self.step(...)` (the audit trail is graded — the
  multi-hop run must be visible in the log).
- Any tool/parse failure → falls back to the deterministic feature recipe;
  pipeline never crashes, FeatureFrame contract always satisfied.
- **Regression guard:** `DataAnalystAgent`'s `feature_frame` is *identical*
  (field-for-field Arrow compare) to `build_features` output on the offline
  snapshot — not "close," identical.
- Offline tests green on MockLLM.

## Tests (new: `tests/test_data_analyst.py`) — green offline on MockLLM

- Regression guard (above): compare the Arrow tables field-for-field.
- Multi-hop: a **scripted** MockLLM emitting a tool-call sequence then `done` →
  assert the steps log shows the hops in order and `analyst_aggregates` is
  populated.
- Degradation: unparsable reply and brain-raises → still returns OK with the
  deterministic frame and an empty/partial aggregates dict.
- Orchestrator wiring: full `orch.run()` on the synthetic snapshot completes OK
  with `data_analyst` in the step order. Update `test_orchestrator.py` (and grep
  the repo for any test asserting the literal `"analytics"` step name).

## Out of scope

- Data validation / quality checks (WA-029).
- New feature *definitions* beyond what the debate needs (keep scope tight).
- The efficiency benchmark (WA-017) — unaffected by this swap.

## Depends on

- WA-029 (DuckDB query layer + dlt ingestion must exist for the agent to call).

## Dev note

The repo `.venv` may be missing deps — run `pip install -r requirements.txt`
(needs `duckdb`, `mcp`, `httpx`, `dlt`, `oss2`) before testing, or the suite
shows ~37 spurious `No module named 'duckdb'` failures that are NOT real.
