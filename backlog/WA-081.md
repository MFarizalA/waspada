---
id: WA-081
state: todo
priority: P1
owner: backend
branch: feature/wa-081-dlt-doc-cleanup
contract_frozen: false
---

# WA-081 · Kill the dlt confusion permanently — docs, deps, docstrings

**Why this exists:** dlt has flip-flopped three times (designed in → dead in
practice → removed WA-047 → questioned again 2026-07-19) because the removal
never propagated to the artifacts people read. The owner asked "don't we use
dlt?" today — the docs lied to him. This ticket makes every surface tell the
same truth. Root cause was EM (Stefanie): removed the code, not the claims.

## The one true sentence (everything must match it)

> **The Data Engineer agent reads OSS parquet directly via DuckDB. There is
> no dlt anywhere in the system. dlt was removed in WA-047 (dead code path)
> and must not be reintroduced without an explicit owner decision.**

## Changes (all small, all mechanical)

1. **`requirements.txt`** — delete the `dlt[duckdb]>=1.4` line (15) and its
   WA-029 comment block (14). Keep the DuckDB line if present.
   After removal: `pip install -r requirements.txt` in a fresh venv still
   lets the full suite pass (dlt is imported nowhere — the AST guard test
   in test_lakehouse.py proves it).
2. **`README.md`** (~lines 152, 306) — replace "dlt + DuckDB" lakehouse
   wording with the true sentence's substance.
3. **`HACKATHON.md`** (~lines 140, 328-336, 465) — same; the Data Engineer
   is "function-calling loop over DuckDB over an Arrow table from OSS",
   NOT "dlt/DuckDB check core". ALSO add this decision block verbatim near
   the architecture section:
   ```
   > **Settled decision (WA-047, owner-approved):** dlt was removed as a
   > dead code path. Data loads via direct parquet→OSS upload
   > (scripts/load_lending_club.py, WA-078) and reads via DuckDB.
   > Do not reintroduce dlt without an explicit owner decision.
   ```
4. **`waspada/agents/data_engineer.py` line 11** — module docstring still
   says "via the lakehouse layer (dlt + ..."; correct it to the true
   sentence. Comment only, zero code change.
5. **Backlog files** (WA-029.md, WA-030.md, BRIEF-2026-07-14.md) — historical,
   do NOT rewrite; but if any line presents dlt as CURRENT design, annotate
   it `(historical — dlt removed WA-047)`.

## Acceptance

- `git grep -in "dlt" -- "*.py" "*.txt" "README.md" "HACKATHON.md"` returns
  ONLY: the guard tests (test_lakehouse.py), the "no dlt" comments
  (data_engineer.py:134,391, data_analyst.py:532), WA-047/069 backlog
  references, and the new HACKATHON decision block.
- Full suite green after requirements.txt change.
- One commit: `chore(wa-081): purge stale dlt references — docs, deps, docstring`.

## Out of scope

- Any code behavior change.
- Re-evaluating whether dlt SHOULD come back (settled — owner's call only).
