---
id: WA-037
state: todo
priority: P2
owner: backend
branch: wa-037-origination-insight
contract_frozen: false
---

# WA-037 · `insight/origination.py` — approve/refer/reject matrix + health

## Goal

The origination decision layer, parallel to `insight/ranking.py` (whose action
semantics are collections-only: `ACTION_BY_BAND` Q5→call… (`ranking.py:46-52`),
NPL buckets (`:63`), NPL/vintage health + alerts (`:115-211`)).

## Files / interfaces

- `waspada/insight/origination.py`:
  - **Decision matrix** band→action: default e.g. Q1/Q2→`approve`,
    Q3→`refer` (human review), Q4/Q5→`reject`. **This must be a
    `RiskPolicy` (WA-032) instance from day one** — a second policy file
    (`origination_policy.json`), NOT a new constant. WA-032's loader/validation
    generalizes: parameterize its allowed-action vocabulary per lane.
  - Optional risk-based pricing hook: `priced_rate = base + band_spread` (spread
    table also policy-owned; label assumptions on-screen like the EL note).
  - `OriginationHealth`: approval rate, band mix, projected default rate of the
    approved book, expected approved volume.
  - Alerts: approval-rate drift / projected-default-rate threshold (policy-owned
    thresholds, mirroring `alerts()` `ranking.py:173-211`).
  - Reuse `rank()`'s sort-skeleton + `to_dashboard_payload()` (`ranking.py:69-101,
    230-253`) — the payload shape stays `DashboardPayload` with lane-appropriate
    `work_list`/health keys.
- The **debate reuses as-is**: the Skeptic contests the riskiest *approve*
  decisions (top-K by p_default among approvals) — same `Dispute` protocol, same
  gate. The human gate action becomes `publish_decisions` for this lane.

## Acceptance

- Deterministic decisions from the policy matrix; editing the origination policy
  JSON changes approve/refer/reject cutoffs with no code change.
- Health + alerts computed on the synthetic snapshot; payload JSON-serializable.
- Mirrors `tests/test_ranking.py` structure.

## Depends on

WA-032 (RiskPolicy machinery), WA-036 (ScoredApplications).
