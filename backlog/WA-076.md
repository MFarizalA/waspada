---
id: WA-076
state: todo
priority: P1
owner: unassigned
branch:
contract_frozen: false
---

# WA-076 · Submission assembly — the final package

**Candidates:** owner (form access + final authority) with Stefanie running the
checklist. This is the gate everything else feeds.

## Goal

Assemble and submit the complete package. Every item below comes from the
official submission requirements — the checklist IS the acceptance criteria.

## The checklist (verbatim from the rules)

- [ ] **Public code repository URL** — contains ALL source code, assets, and
  instructions required for the project to be functional. Repo must be
  **public** (currently private — owner flips the switch after WA-020's
  secrets sweep is re-confirmed by WA-071 item 5).
- [ ] **Open source license** — detectable and visible at the top of the
  repo page (About section). MIT LICENSE file at root; GitHub must
  auto-detect it (WA-072 verifies detection).
- [ ] **Proof of Alibaba Cloud deployment** — a link to a code file in the
  repo demonstrating use of Alibaba Cloud services and APIs
  (`deploy/iac/main.tf` permalink — WA-073 confirms the exact URL).
- [ ] **Architecture diagram** — clear visual of how Qwen Cloud connects to
  backend, database, frontend (WA-073 delivers the file; linked from README).
- [ ] **~3-minute demo video** — public on YouTube/Vimeo/Facebook Video
  (WA-074 scenario; owner records + uploads; link goes in the form AND in
  README hero).
- [ ] **Text description** — features and functionality
  (`docs/submission-description.md` from WA-073, paste-ready).
- [ ] **Track identification** — **Track 3: Agent Society** (state it in the
  form, the description, and the README badge).
- [ ] **Optional: blog/social post URL** — Blog Post Prize eligibility.
  Owner's call whether we do one; if yes, draft the journey-with-QwenCloud
  post (Claude can draft, owner publishes).

## Blocking dependencies

WA-067 (custom domain — or the documented fcapp.run fallback), WA-070
(review), WA-071 (sanity pass), WA-072 (branding), WA-073 (docs + diagram +
description), WA-074 (video). The live-qwen prod fix (owner decision
pending) gates the video's money shot.

## Acceptance

Every box ticked with evidence (URLs, permalinks, upload links), the form
submitted, and a confirmation captured (screenshot/email). Stefanie verifies
each artifact link works from an unauthenticated browser before submission —
judges won't be logged in.

## Out of scope

- Post-submission changes (freeze the repo at submission; fixes after that
  are a new conversation).
