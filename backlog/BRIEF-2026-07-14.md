# Engineering brief — sprint sessions wrap-up & next steps

**To:** Stefanie (EM — owns ticket creation/priority) · **From:** Bimo (backend)
**Date:** 2026-07-14 · **Repo state:** `main` @ `aa918f3`, pushed, all green
**Context:** the hackathon is ongoing — this wraps up the recent working
sessions, not the competition.

---

## TL;DR

Over the recent working sessions the Collections lane went from "core demo" to
"hardened product": the sixth society member (Data Analyst) shipped, the debate
now streams live over SSE, every run ships an SLS audit trail, the dashboard is
bilingual (EN/中文) with a China-market visual language, and the risk vocabulary
was renamed to analyst-readable levels with FICO-style score pairing
(`0.98 (Very High)`). 324 tests green offline; every merge was full-suite
verified. **Two things need you:** (1) Alibaba creds to unblock the WA-018
deploy — with the hackathon still live, the public `*.fcapp.run` URL is
deployment proof for the Devpost checklist, so this is the most time-sensitive
item; (2) a priority call — I recommend a ~2-day hardening pass (CI + security)
before the team starts the Origination lane, whose specs are frozen and ready
in WA-032..039.

## Shipped these sessions (all merged to `main`, pushed)

| Feature | Ticket | Merge | What it is |
|---|---|---|---|
| Data Analyst agent | WA-030 | `8f8e2fb` + fix `1f1d1e5` | qwen3.7-plus function-calling loop over DuckDB SQL; deterministic FeatureFrame regression-pinned; post-review fix lets `correlation`/`distribution` reach the FeatureFrame columns the debate cites |
| SSE live debate | WA-022 | `4b5180c` | `GET /api/run/stream` — judges/analysts watch rounds arrive live; frontend hook was already built, backend wired the orchestrator's existing hooks |
| SLS audit stream | WA-023 | `5940575` | every Step/Handoff/DisputeRound shipped to Simple Log Service, fail-safe local JSONL fallback (an SLS outage can never break a run) |
| Qwen 500 fix | — | `4dbbded` | "Run live (Qwen)" without a key returned a bare 500; now a clean 503 with an actionable message |
| Env-configurable Qwen tiers | — | `3657d71` | `QWEN_MODEL_FLASH/PLUS/MAX` override the three cognitive-load tiers; defaults unchanged |
| Bilingual + CN redesign | WA-031 | `c9ef50d` | EN/中文 live toggle (persisted), imperial red/gold design system, megamenu banner, CJK-aware type |
| Risk-level rename + pairing | — | `aa918f3` | Q1..Q5 → Very Low..Very High everywhere (code, fixtures, tests, docs); `p_default` paired with the label FICO-style in the drawer and the debate clash line; color/label logic consolidated into one module (was duplicated 3 ways) |
| Worktree tooling | — | `c6112d8` | `scripts/wt.sh` — one agent per isolated worktree, after a real two-agents-one-directory incident |
| Requirements frozen | WA-032..039 | `4dd3a80` | RiskPolicy matrix + the full Origination lane, spec'd to file:line seams so any engineer starts cold |

Backlog hygiene: this commit also reconciles ~13 stale ticket states
(shipped work that still read `todo`/`deferred`) so the board matches reality.
Two honest nuances: WA-003's data acquisition ran via BigQuery and was later
migrated to OSS (deliverable achieved, destination changed); WA-005 shipped as
sklearn CPU rather than cuML GPU (deliberate — identical train/predict
signatures keep the GPU estimator a drop-in swap).

## Quality posture

- **324 passed, 7 skipped, 1 xfailed** — fully offline (MockLLM default, creds
  stripped in conftest; live smoke tests skip cleanly without creds).
- Frontend: `tsc -b` clean, `vite build` green (55 modules). No frontend unit
  test runner exists — typecheck is the mechanical gate.
- Discipline that caught real bugs: running the *full* suite after every merge
  (not per-branch) surfaced a cross-branch integration failure (WA-030's new
  agent consumed WA-022's scripted test brain) before it reached `main`.
- **Gap: none of this is enforced** — there is no CI; the suite runs only when
  someone remembers to run it.

## Open risks / tech debt (each verified against code this week)

| # | Item | Evidence | Exposure |
|---|---|---|---|
| 1 | JWT secret falls back to an insecure dev default (28 bytes) | `api/auth.py:97-101` | **Must fix before any public deploy** |
| 2 | RDS open to the internet | `deploy/iac/main.tf` `security_ips = ["0.0.0.0/0"]` | Same — deploy blocker |
| 3 | No CI pipeline | `.github/workflows` absent | Every push is unguarded |
| 4 | `analyst_aggregates` produced but consumed nowhere | `data_analyst.py` output; zero downstream readers | WA-030's stated purpose (backing the MCP evidence base) isn't closed end-to-end |
| 5 | Expected Loss is frontend-label-only | no backend EL computation exists (`grep expected_loss **/*.py` → 0) | WA-024 half-done; the EL column renders only if a payload carries it |
| 6 | dlt lakehouse path is a broken stub | `waspada/data/lakehouse.py:145` calls nonexistent `dlt.readers.filesystem`; loaded data never lands in the returned connection | Real OSS-via-dlt ingestion would fail; offline paths unaffected |
| 7 | Native `tools`/`tool_calls` function calling absent | JSON-mode prompt-and-parse only | HACKATHON.md's biggest 🟡 — a live rubric item ("sophisticated QwenCloud API use") while the hackathon is ongoing |
| 8 | SSE stream has no keepalive; client disconnect can't cancel the run thread | `api/main.py` stream route | Slow real-Qwen runs behind proxies may idle-timeout |
| 9 | DuckDB↔RDS federation (`postgres_scanner`) unbuilt | no references in code | HACKATHON 🟡 (lakehouse story) |

## Recommended next steps (priority order)

### P0 — Foundation (~half day total; protects everything after it)
1. **CI pipeline** (~1–2h): GitHub Actions running `pip install -r requirements.txt`
   + `pytest -q` (offline by design) and `cd dashboard && npm ci && npm run build`.
   The suite is trustworthy; it just needs to run on every push.
2. **Deploy-blocking security** (~2h): require a ≥32-byte `WASPADA_JWT_SECRET`
   outside dev (hard-fail, not a warning); tighten `security_ips` in the IaC.

### P1 — Product increment (~1.5 days total)
3. **WA-032 RiskPolicy matrix** (~half day) — land this *first*: spec frozen,
   and WA-037 (origination decisions) reuses its machinery. Analysts edit the
   band→action matrix + alert thresholds in a validated JSON file, no code.
4. **Wire `analyst_aggregates` → MCP evidence base** (~2h) — closes debt #4;
   what the Skeptic cites becomes what the Data Analyst actually computed.
5. **WA-024 EL backend** (~2h) — `expected_loss = p_default × 0.45 ×
   outstanding_principal` into the payload; the dashboard already renders it
   when present. The most credit-credible number we can show a risk committee.
6. **WA-018 FC deploy** — everything is ready (IaC, Dockerfile, runbook);
   **blocked solely on Alibaba creds** (your ask #1 below).

### P2 — The Origination lane (the next big product bet)
Order: **WA-034 → 035 → 036 → 037 → 033 → 038 → 039** (contract → features →
model → decisions → wiring → data agents → dashboard). Specs frozen 2026-07-12
with exact file:line seams; the debate engine reuses verbatim. Note the honesty
rail baked into WA-034/035: the source data has funded loans only, so the label
is funded-then-defaulted — no reject-inference claims.

### P3 — Stretch / rubric polish
Native `tools`/`tool_calls` function calling (turns the biggest HACKATHON 🟡
into ✅), dlt lakehouse fix (#6), DuckDB↔RDS federation (#9), SSE keepalive (#8).

## Asks for Stefanie

1. **Alibaba AccessKey pair + DashScope key for WA-018** — the only blocker on
   a public `*.fcapp.run` URL.
2. **Priority call:** hardening-first (P0+P1, ~2 days) then Origination — my
   recommendation, since Origination builds directly on the policy machinery
   and deploy posture — or Origination immediately if there's a market reason.
3. **Owner assignments** for P1/P2 (WA-039 is frontend — Kirana; the rest are
   backend-shaped; QA gates per lane — Reza).
