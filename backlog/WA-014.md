---
id: WA-014
state: done
priority: P0
owner: backend
branch:
contract_frozen: true
---

# WA-014 · RiskAuditor challenge round + dispute wiring + agent_dialogue

## Goal

Build the Skeptic — `RiskAuditorAgent` (`qwen3.6-flash` + native Qwen function
calling). After the classical-ML model scores 100% of the book, the Skeptic
audits the **top-K riskiest accounts** (K=8 default) and, where its independent
view diverges from the model's band, opens a `Dispute`. Wire disputes into the
orchestrator (`Status.DISPUTED` branch + `resolve_risk_dispute` gate action) and
thread the serialized `agent_dialogue` array into the `DashboardPayload`.

This ticket is Round 1 only (challenge). Rebuttal + Arbiter (Rounds 2-3) are
WA-016. MCP tools the Skeptic cites are WA-015. But the dispute dataflow and
payload contract land here — everything downstream builds on it.

## The frozen contract (do not design — implement to match)

The dispute-record shape is already locked in three places: HACKATHON.md
§ dispute record, `dashboard/src/types.ts` (DisputeRecord/DisputeRound), and the
committed fixture `dashboard/fixtures/sample-payload.json`. The backend must emit
EXACTLY this. Backend-side contract work in this ticket:

1. `waspada/agents/protocol.py` — add additive fields to existing dataclasses:
   - `DisputeRound.model: Optional[str] = None` (e.g. "qwen3.6-flash"; None = deterministic speaker)
   - `DisputeRound.evidence: List[str] = field(default_factory=list)` (cited feature values / portfolio stats)
   - `Dispute.model_band: str = ""` (the Actuary's risk level, e.g. "Very High")
   - `Dispute.auditor_view: str = ""` (the Skeptic's read: "Low"|"Medium"|"High")
2. `waspada/schema.py` — add `agent_dialogue: Optional[List[Dict]] = None` to
   `DashboardPayload` (additive optional key — older payloads without it stay valid).
3. The frontend already renders this shape; do not change field names.

## Files / interfaces

- `waspada/agents/risk_auditor.py` (NEW) — `RiskAuditorAgent(Agent)`:
  - `name = "risk_auditor"`, `role = "audit top-K scores and open disputes"`
  - Constructor: `(llm, *, k: int = 8)` (k = how many top accounts to audit)
  - `run(context)`: read the `scored_accounts` handle from `prior_results`;
    take top-K by `p_default`; for each, produce an auditor_view + confidence;
    if bands disagree (model_band vs auditor_view diverge — define the rule:
    e.g. model Very High + auditor Low/Medium = dispute; Very High + auditor High = no dispute),
    open a `Dispute` with a Round 1 `DisputeRound` (speaker=risk_auditor,
    model=brain name, claim, confidence, evidence).
  - Parses JSON-mode LLM output (`response_format: json_object`); on parse
    failure → no dispute opened, logged, pipeline continues (graceful degrade).
  - Register its tools (MCP in WA-015; here it can carry a local
    `portfolio_stats`/`lookup_account` stub via `register_tool`, same pattern as
    ingest's `fetch`).
- `waspada/agents/insight.py` — accept an optional `disputes: List[Dispute]`
  kwarg; serialize them (dataclasses → dicts matching the frozen shape) into
  `payload["agent_dialogue"]`.
- `waspada/agents/orchestrator.py`:
  - Insert `RiskAuditorAgent` into the step order AFTER risk_model, BEFORE insight.
  - When the auditor opens disputes, set the run's terminal state to
    `Status.DISPUTED` and route to gate action `resolve_risk_dispute` (distinct
    from `publish_work_list` in the audit log).
  - Thread the disputes list into the insight agent call.
- `waspada/agents/__init__.py` — export `RiskAuditorAgent`.
- `api/main.py` — fix the step-collection so auditor steps surface (the current
  `orch.steps` collection is orchestrator-only; each agent's own `.steps` need
  surfacing for the audit trail).

## Acceptance

- `python -m waspada.agents --lane collections --auto-approve` (mock brain)
  produces a payload whose `agent_dialogue` matches the frozen shape (can be
  empty if the mock brain doesn't open disputes; shape must be valid either way).
- With a scripted `MockLLM` that returns a challenge JSON, the pipeline opens a
  dispute, sets `Status.DISPUTED`, and routes to `resolve_risk_dispute`.
- A dispute opened with an unparsable LLM reply is logged and skipped — pipeline
  completes (graceful degrade test).
- `DashboardPayload` with `agent_dialogue` present round-trips through
  `json.dumps`; the existing dashboard fixture still validates.
- All existing tests stay green; new tests cover: dispute-opened path,
  no-dispute path, parse-degradation path, agent_dialogue shape.

## Out of scope

- MCP server (WA-015) — the auditor can run with a local stub tool here.
- Rebuttal / Arbiter Rounds 2-3 (WA-016) — Round 1 only; an opened dispute with
  no rebuttal resolves to "escalated" for now (documented interim state).
- Live Qwen testing (needs DASHSCOPE_API_KEY from owner) — mock-brain paths only.
- SLS logging (WA-023), Expected Loss (WA-024).

## Notes for the worker

- The orchestrator's `_build_agents` and `COLLECTIONS_STEP_ORDER` both change.
- The `resolve_risk_dispute` gate action is a NEW action string — make sure
  `ApprovalGate.request(action, rationale)` accepts it and logs distinctly.
- Keep the mock-brain path deterministic: a scripted MockLLM must be able to
  force each path (open / no-dispute / parse-fail) for QA (WA-020).
