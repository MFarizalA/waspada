# CPU-only deps for the API server
fastapi>=0.115
uvicorn[standard]>=0.30
scikit-learn>=1.5
pyarrow>=17
pandas>=2.2
numpy>=1.26
python-dotenv>=1.0
oss2>=2.18
openai>=1.0
mcp>=1.0
PyJWT>=2.8
passlib[bcrypt]>=1.7
email-validator>=2.0
# ApsaraDB RDS PostgreSQL driver (optional for local dev / tests, required when
# DATABASE_URL points at postgres:// — see api/db.py).
psycopg2-binary>=2.9
